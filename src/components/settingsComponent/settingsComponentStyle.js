import { StyleSheet } from "react-native";

export default StyleSheet.create({
  firstContainer: {
    flexDirection: "row",
    marginBottom: 10,
    alignItems: "center",
  },

  text: {
    fontSize: 21,
  },
});
